DB_HOST=localhost
DB_PORT=1433
DB_NAME=TTM_DEV
DB_USER=sa
DB_PASSWORD=your_password_here
# Set to true when connecting to Azure SQL or any TLS-enforced instance
DB_ENCRYPT=false
# Set to false in production when a CA-signed certificate is in use
DB_TRUST_CERT=true
PORT=3001
